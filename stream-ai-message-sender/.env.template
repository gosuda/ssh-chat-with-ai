# Gemini API Key (required)
GEMINI_API_KEY=your-gemini-api-key-here

# gRPC Server Address (required)
GRPC_SERVER_ADDRESS=localhost:3333

# Optional: TLS target name override for certificate verification
# Use 'localhost' if your certificate is issued for localhost
# GRPC_TLS_TARGET_NAME=localhost

# Optional: Directory containing TLS certificates (defaults to ./certs)
#CERTS_DIR=./certs

# Optional: Custom path to the AI client's private key (defaults to ./certs/ai_grpc_client.key)
#PRIVATE_KEY_PATH=./certs/ai_grpc_client.key

# Optional: Custom path to the gRPC server certificate chain (defaults to ./certs/grpc_server.cert)
#GRPC_SERVER_CERT_CHAIN_PATH=./certs/grpc_server.cert

# Optional: Override Gemini client configuration file (defaults to ./conf/participants.json)
#GEMINI_CLIENT_CONFIG_PATH=./conf/participants.json

# Optional: Probability (0.0-1.0) that any AI responds (defaults to 1.0)
#AI_RESPONSE_PROBABILITY=1.0

# Optional: Interval (in seconds) for sending info messages to clients
# defaults to 180, min 60.
#INFO_MESSAGE_INTERVAL=180
